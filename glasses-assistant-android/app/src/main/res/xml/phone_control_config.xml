<?xml version="1.0" encoding="utf-8"?>
<!-- Assistive service configuration. It reads on-screen text so the
     assistant can describe the screen, and performs a single gesture or
     click per voice turn. It retrieves window content and can perform
     gestures; it does not log keystrokes or run continuously in the
     background beyond a triggered turn. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_description"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows|flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:canPerformGestures="true"
    android:notificationTimeout="100"
    android:settingsActivity="com.kits.glasses.MainActivity" />
